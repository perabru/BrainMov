<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brunosjc.brainmov">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ProntuarioAdmActivity"/>
        <activity android:name=".activities.RecuperarSenhaActivity"/>
        <activity android:name=".activities.VideosPacienteActivity"/>
        <activity android:name=".activities.PainelAdmActivity"/>
        <activity android:name=".activities.LoginAdmActivity"/>
        <activity android:name=".activities.PrincipalPacienteActivity"/>
        <activity android:name=".activities.CadastroActivity"/>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>